//const isLetter = code => code > 0 && code < 27;

//const charToCode = char => char.charCodeAt(0) - 64;

// function alphabetPosition(text) {
//   return [...text.toUpperCase()].map(charToCode).filter(isLetter).join(' ');
// }

// function DNAStrand(dna) {
//   const qwe = {
//     A: 'T',
//     T: 'A',
//     C: 'G',
//     G: 'C',
//   };

//   return [...dna].map(element => qwe[element]).join('');
// }
// console.log(DNAStrand('ATTGC'));
// console.log(DNAStrand('AAAAA'));
